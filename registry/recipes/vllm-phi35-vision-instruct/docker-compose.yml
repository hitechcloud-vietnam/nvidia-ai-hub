services:
  vllm:
    image: vllm/vllm-openai:v0.17.1-aarch64-cu130
    container_name: spark-ai-hub-vllm-phi35-vision-instruct
    ports:
      - "9001:8000"
    volumes:
      - huggingface-cache:/root/.cache/huggingface
    environment:
      - HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}
      - TRANSFORMERS_OFFLINE=${TRANSFORMERS_OFFLINE:-0}
    ipc: host
    command:
      - --model
      - microsoft/Phi-3.5-vision-instruct
      - --port
      - "8000"
      - --max-model-len
      - "4096"
      - --limit-mm-per-prompt
      - image=1
      - --trust-remote-code
      - --enable-prefix-caching
      - --gpu-memory-utilization
      - "0.40"
      - --served-model-name
      - phi-3.5-vision-instruct
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped

volumes:
  huggingface-cache: