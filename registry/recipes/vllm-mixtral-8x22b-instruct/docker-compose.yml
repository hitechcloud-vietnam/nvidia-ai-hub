services:
  vllm:
    image: vllm/vllm-openai:v0.17.1-aarch64-cu130
    container_name: spark-ai-hub-vllm-mixtral-8x22b-instruct
    ports:
      - "9001:8000"
    volumes:
      - huggingface-cache:/root/.cache/huggingface
    environment:
      - HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}
      - TRANSFORMERS_OFFLINE=${TRANSFORMERS_OFFLINE:-0}
    ipc: host
    command:
      - --model
      - mistralai/Mixtral-8x22B-Instruct-v0.1
      - --port
      - "8000"
      - --tokenizer-mode
      - mistral
      - --max-model-len
      - "65536"
      - --enable-prefix-caching
      - --enable-auto-tool-choice
      - --tool-call-parser
      - mistral
      - --gpu-memory-utilization
      - "0.82"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped

volumes:
  huggingface-cache:
