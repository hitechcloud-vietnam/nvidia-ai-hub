# Control UI token. Change this before first launch.
OPENCLAW_GATEWAY_TOKEN=change-me-openclaw-token

# Use `lan` for published Docker ports. OpenClaw will persist its config and
# workspace under `registry/recipes/openclaw/data/openclaw/`.
OPENCLAW_GATEWAY_BIND=lan