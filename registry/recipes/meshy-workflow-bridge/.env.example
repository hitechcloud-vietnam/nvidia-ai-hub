# Meshy Workflow Bridge runtime configuration
# MESHY_API_KEY is optional at container start; the ComfyUI-Meshy node still expects the key to be entered in the workflow graph.

COMFYUI_PORT=8189
CLI_ARGS=--listen 0.0.0.0 --port 8188
MESHY_API_KEY=
GRADIO_ANALYTICS_ENABLED=False
