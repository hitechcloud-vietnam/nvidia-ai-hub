{
  "name": "nvidia-ai-hub-frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Electron and web frontend for NVIDIA AI Hub by Pho Tue SoftWare Solutions JSC.",
  "author": "Pho Tue SoftWare And Technology Solutions Joint Stock Company",
  "license": "SEE LICENSE IN ../LICENSE",
  "homepage": "https://github.com/hitechcloud-vietnam/nvidia-ai-hub",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hitechcloud-vietnam/nvidia-ai-hub.git"
  },
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite build && vite",
    "build": "vite build",
    "build:web": "vite build",
    "build:backend": "node ./electron/scripts/build-backend.mjs",
    "build:icons": "node ./electron/scripts/generate-icons.mjs",
    "clean:release": "node ./electron/scripts/clean-release.mjs",
    "desktop:prepare": "npm run clean:release && npm run build:web && npm run build:icons && npm run build:backend && npm run electron:ensure",
    "electron:ensure": "node ./electron/scripts/ensure-electron.mjs",
    "lint": "eslint .",
    "preview": "vite preview",
    "desktop:dev": "npm run build:web && npm run build:icons && npm run electron:ensure && electron .",
    "desktop:pack": "npm run desktop:prepare && electron-builder --dir",
    "desktop:dist": "npm run desktop:prepare && electron-builder",
    "desktop:dist:win": "npm run desktop:prepare && electron-builder --win nsis -c.artifactName=\"${productName}-${version}-win-${arch}-setup.${ext}\" && electron-builder --win portable -c.artifactName=\"${productName}-${version}-win-${arch}-portable.${ext}\" && electron-builder --win zip -c.artifactName=\"${productName}-${version}-win-${arch}.${ext}\" && electron-builder --win msi -c.artifactName=\"${productName}-${version}-win-${arch}.${ext}\"",
    "postinstall": "npm run electron:ensure"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "i18next": "^26.0.6",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-i18next": "^17.0.4",
    "react-router-dom": "^7.14.1",
    "recharts": "^3.8.1",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "@electron/notarize": "^3.1.0",
    "@eslint/js": "^10.0.1",
    "@tailwindcss/vite": "^4.2.2",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "electron": "^41.2.1",
    "electron-builder": "^26.0.12",
    "eslint": "^10.2.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.5.0",
    "icon-gen": "^5.0.0",
    "tailwindcss": "^4.2.0",
    "vite": "^8.0.8"
  },
  "build": {
    "appId": "com.photue.nvidia-ai-hub",
    "productName": "NVIDIA AI Hub",
    "directories": {
      "output": "release"
    },
    "artifactName": "${productName}-${version}-${os}-${arch}.${ext}",
    "files": [
      "dist/**/*",
      "electron/**/*",
      "!electron/backend-dist{,/**/*}",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "electron/backend-dist",
        "to": "backend"
      },
      {
        "from": "../registry",
        "to": "app-bundle/registry"
      },
      {
        "from": "dist",
        "to": "app-bundle/frontend/dist"
      }
    ],
    "asar": true,
    "npmRebuild": false,
    "afterSign": "electron/notarize.cjs",
    "win": {
      "icon": "build-assets/icon.ico",
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "NVIDIA AI Hub"
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.developer-tools",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "identity": "${CSC_NAME}",
      "icon": "build-assets/icon.icns",
      "entitlements": "electron/entitlements.mac.plist",
      "entitlementsInherit": "electron/entitlements.mac.plist"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb",
        "tar.gz"
      ],
      "category": "Development",
      "icon": "public/logos/nvidia.png",
      "synopsis": "Desktop launcher for NVIDIA AI Hub",
      "description": "Desktop distribution that bundles the NVIDIA AI Hub frontend and a local FastAPI backend runtime."
    }
  }
}
